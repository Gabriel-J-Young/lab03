import argparse
import math
import pandas as pd

def random_forest(D, A, N, m, k, DecisionTreeImplementation):
    # D - dataset
    # A - list of attributes
    # N - number of decision trees in the forest
    # m - number of attributes in each tree
    # k - number of data point to use for constructing the tree
    # DecisionTreeImplementation - decision tree induction 

    #run C45
    forest = {}
    
    return forest

def RFClassify(Forest, x):
    prediction = {}

